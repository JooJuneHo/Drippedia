# Drippedia — 핸드드립 레시피 공유 서비스 — v1 요구사항 (한 페이지)

개인 사이드 프로젝트. 목표는 완벽한 스펙이 아니라, 지금 뭘 만들고 뭘 나중으로 미루는지 헷갈리지 않는 것.

## 1. 핵심 유저 시나리오

1. (필수) 구글/카카오 계정으로 로그인한다.
2. (필수) 원두 정보, 도구, 비율, 물 온도, 단계별 푸어 타이밍을 입력해 레시피를 등록한다.
3. (필수) 등록된 레시피 목록을 훑어보고, 도구/원두 기준으로 필터링해서 원하는 걸 찾는다.
4. (필수, 핵심 차별점) 레시피 상세 화면에서 "브루잉 시작"을 누르면, 단계별 타이머가 순서대로 안내한다.
5. (필수) 내가 등록한 레시피만 모아서 본다.

## 2. v1 기능 목록 (우선순위)

| 우선순위 | 기능 | 비고 |
|---|---|---|
| P0 | 구글/카카오 소셜 로그인 | User에 provider, providerId 추가 필요 |
| P0 | 레시피 등록 | 원두명, 로스터, 원산지, 로스팅 정도, 도구, 커피/물 양, 온도, 분쇄도, PourStep 1개 이상 |
| P0 | 레시피 목록 조회 | 최신순 기본 |
| P0 | 레시피 상세 조회 | PourStep 순서대로 표시 |
| P0 | 브루잉 타이머 | PourStep의 startTimeSeconds 기준 순차 안내 |
| P1 | 도구(BrewMethod)/원두 필터 | 목록 화면에서 필터링 |
| P1 | 내 레시피 모아보기 | authorId 기준 |

## 3. v1 제외 범위 (의도적으로 나중으로 미룸)

- 이메일/비밀번호 자체 로그인 (소셜 로그인만 지원)
- 댓글, 좋아요, 팔로우 등 소셜 기능
- 원두(Bean) 마스터 데이터 정규화, 자동완성
- 실제로 내려본 기록/맛 평가 히스토리 (BrewLog)
- 네이티브 모바일 앱 (반응형 웹으로 우선 커버)
- DB 레벨 FK 제약, 테이블 간 JPA 연관관계 (의도적으로 애플리케이션 레벨에서만 관리)

## 4. 완료 기준 (Definition of Done)

- **로그인**: 구글 또는 카카오 계정으로 로그인하면 User가 없으면 생성되고, 있으면 기존 계정으로 연결된다.
- **레시피 등록**: 필수 필드(원두명, 도구, 비율, 온도, PourStep 1개 이상) 입력 후 저장하면 목록에 즉시 노출된다.
- **브루잉 타이머**: 시작 버튼 클릭 시 각 단계가 startTimeSeconds 기준으로 순서대로 안내되고, 마지막 단계 후 완료 표시된다.
- **목록/필터**: 도구 또는 원두 조건으로 필터링 시 해당 조건에 맞는 레시피만 노출된다.
- **내 레시피 모아보기**: 로그인한 사용자가 본인이 등록한 레시피만 모아볼 수 있다.

## 5. 이번 결정으로 바뀐 것

- User 엔티티에 `provider`(GOOGLE/KAKAO), `providerId` 필드 추가 필요 → OAuth2 로그인 연동 작업이 다음 단계에 추가됨.
- 필터/내 레시피 모아보기가 P1로 v1에 포함 → RecipeRepository의 `findByAuthorId`, `findByBrewMethod`는 이미 준비돼 있음.
